#include <stdio.h>
/**
 * main - This indentifier does not take in any arquements
 * Return: 0(success)
 */
int main(void)
{
	char number;

	for (number = 0; number < 10; number++)
		{
		putchar(number);
		}
		putchar('\n');
		return (0);
}
