#include <stdio.h>
/**
 * main - This section takes in no arguement
 * Return:0(success)
 */
int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter--)
		{
		putchar(letter);
		}
		putchar('\n');
		return (0);
}

