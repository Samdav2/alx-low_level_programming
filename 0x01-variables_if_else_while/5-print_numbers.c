#include <stdio.h>
/**
 * main - This section does not accept arguements
 * Return:0(success)
 */
int main(void)
{
	int number;

	for (number = 0; number < 10; number++)
		{
		printf("%d", number);
		}
		printf("\n");
	return (0);
}
