#include <stdio.h>
/**
 * main - Fixing ad infinite loop
 * Return: 0(success)
 */
int main(void)
{
	int i;

	printf("Infinite loop incoming :(\n");

	i = 0;
	/*
	 * while (i > 10)
	 * {
	 * putchar(i);
	 * }
	 */
	printf("Ifinite loop avoided! \\o/\n");

	return (0);
