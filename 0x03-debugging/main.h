#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * positive_or_negative - check if assign number is positive or negative
 */
void positive_or_negative(int i);
int main(void);
int largest_number(int a, int b, int c);
void print_remaining_days(int month, int day, int year);
int convert_day(int month, int day);

#endif /* MAIN_H */
