#include "main.h"
/**
 * _abs - Computes the value of an integer
 * Return: 0(success)
 * @n: Hols a value
 */
int _abs(int n)
{

	if (n > 0)
	{
	return (n);
	}
	else if (n < 0)
	{
	return (-n);
	}
	else
	{
	return (n);
	}
}
